# James Garden Care Relay Endpoint

This repository publishes the current encrypted relay endpoint used by the
sendable standalone client. It contains no game data, account data, database
credentials, or relay credentials.
